import React from "react";
import { ProductDashboard } from "@/components/products/ProductDashboard";

const QuizzDashboard: React.FC = () => {
  return <ProductDashboard productType="quizz" />;
};

export default QuizzDashboard;
