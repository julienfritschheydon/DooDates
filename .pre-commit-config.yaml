repos:
  - repo: local
    hooks:
      # TOUT DÉSACTIVÉ - Pour test rapide E2E
      # - id: ggshield
      #   name: ggshield secret scan (pre-commit)
      #   entry: ggshield secret scan pre-commit
      #   language: system
      #   types: [text]
      #   stages: [pre-commit]
      # - id: eslint
      #   name: ESLint
      #   entry: npm run lint -- --max-warnings=0
      #   language: system
      #   types: [file]
      #   files: \.(js|jsx|ts|tsx)$
      #   stages: [pre-commit]
      # - id: e2e-critical-check
      #   name: 🚨 E2E Critical Configuration Check
      #   entry: node scripts/pre-commit-e2e-check.cjs
      #   language: system
      #   types: [text]
      #   files: (scripts/start-e2e-server.cjs|\.github/workflows/.*\.yml|Docs/ARCHITECTURE/E2E-NODE_ENV-CONFIGURATION.md|tests/e2e/.*\.(spec|test)\.(ts|js))
      #   stages: [pre-commit]
      #   pass_filenames: false
      #   always_run: true

