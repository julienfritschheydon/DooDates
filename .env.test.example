# Environment variables for E2E tests
# Copy this file to .env.test and fill in actual values
# IMPORTANT: Never commit .env.test to git!

# Supabase Configuration (required for integration tests)
# Note: Use same values as production for integration tests
VITE_SUPABASE_URL=https://outmbbisrrdiumlweira.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Integration Test Configuration  
INTEGRATION_TEST_PASSWORD=TestPassword123!

# Gemini API (TEST_MODE for tests)
VITE_GEMINI_API_KEY=TEST_MODE

# Force TEST_MODE for all test files
NODE_ENV=test
