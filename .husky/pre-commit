#!/bin/sh
echo "🔍 DooDates - Validation RAPIDE pre-commit..."

# Détection branche courante
BRANCH=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "main")

# 1. Audit Data-testid (Boutons/Accessibilité) - ULTRA RAPIDE (<1s)
echo "🔍 Audit Data-testid (Boutons)..."
node scripts/data-testid-auditor.cjs --fix
if [ $? -ne 0 ]; then
  echo "❌ Boutons sans data-testid détectés et non-corrigibles automatiquement - Commit bloqué"
  exit 1
fi
# Ré-ajouter les fichiers modifiés par l'auditeur s'il y en a
git add src

# 2. Linting (Strict) - MOYEN (~5-10s)
echo "🔍 ESLint (Strict)..."
npm run lint -- --quiet
if [ $? -ne 0 ]; then
  echo "❌ Erreurs lint détectées - Commit bloqué"
  echo "💡 Pour corriger automatiquement: npm run lint:fix"
  exit 1
fi

# 3. Formatage (Prettier) - RAPIDE (~2-3s)
if [ "$NO_FORMAT" != "1" ]; then
  echo "🎨 Vérification formatage (fichiers modifiés)..."
  STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACMR | grep -E "\.(js|ts|tsx|jsx|json|css|html|md)$")
  if [ -n "$STAGED_FILES" ]; then
    # shellcheck disable=SC2086
    npx prettier --check $STAGED_FILES
    if [ $? -ne 0 ]; then
      echo "❌ Fichiers modifiés non formatés - Commit bloqué"
      echo "💡 Pour corriger: npx prettier --write [fichiers]"
      exit 1
    fi
  else
    echo "ℹ️ Aucun fichier à formater (skip)"
  fi
else
  echo "⏭️ Formatage ignoré (NO_FORMAT=1)"
fi

# 4. ggshield (Secrets) - Sécurité
if command -v ggshield >/dev/null 2>&1; then
  echo "🛡️ ggshield: scan des secrets..."
  ggshield secret scan pre-commit
  if [ $? -ne 0 ]; then
    echo "❌ Secrets détectés - Commit bloqué"
    exit 1
  fi
else
  echo "ℹ️ ggshield non installé (skip)"
fi

# 5. Type-check (TypeScript) - MOYEN (~15s)
# On le garde car c'est critique pour éviter les bugs de compilation
echo "🔍 Vérification TypeScript..."
npm run type-check
if [ $? -ne 0 ]; then
  echo "❌ Erreurs TypeScript - Commit bloqué"
  exit 1
fi

# NOTE: Les tests unitaires et E2E sont supprimés du pre-commit pour plus de rapidité.
# Ils restent obligatoires dans le pre-push et en CI.

echo "✅ Pre-commit validé - Commit autorisé"
exit 0
