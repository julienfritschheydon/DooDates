# DooDates - Spécifications Techniques Phase 2

**Document créé le 23 juin 2025**

## Vue d'Ensemble

### Objectif Phase 2
Transformer l'interface frontend actuelle (localStorage) en application mobile-native avec backend Supabase et interface de vote révolutionnaire.

### Architecture Cible - Mobile-First
```
Frontend Mobile-Native (React + Vite)
├── Interface PollCreator (✅ Terminée Mobile)
├── VotingInterface (🎯 PRIORITÉ - Mobile Révolutionnaire)
├── Dashboard Mobile (🆕 À créer)
└── PWA Features (🆕 Installable + Offline)

Backend (Supabase Mobile-Optimized)
├── Database (PostgreSQL + RLS Mobile)
├── Authentication (Google OAuth Mobile)
├── IA Conversationnelle (OpenAI GPT-4)
└── Push Notifications (PWA)

Interface de Vote Mobile (PRIORITÉ #2)
├── Touch Gestures Avancées
├── Haptic Feedback
├── Micro-animations
└── Synchronisation Automatique

IA Conversationnelle (PRIORITÉ #4)
├── OpenAI GPT-4 Integration
├── Parser Langage Naturel
├── Interface Chat Mobile
└── Fallback Création Manuelle

Services Externes
├── Push Notifications (PWA)
├── Analytics Mobile (Supabase)
└── Monitoring (Sentry Mobile)
```

---

## Phase 2 : Spécifications Détaillées (8 semaines)

### Semaines 1-2 : Backend Foundation

#### 1.1 Setup Supabase
**Objectif :** Infrastructure backend opérationnelle

**Configuration Projet :**
- **Nom projet :** `doodates-prod`
- **Région :** Europe (eu-west-1)
- **Plan :** Pro (25$/mois - nécessaire pour RLS avancé)
- **Environnements :**
  - `doodates-dev` (développement)
  - `doodates-staging` (tests)
  - `doodates-prod` (production)

**Sécurité :**
- Row Level Security (RLS) activé sur toutes les tables
- Policies restrictives par défaut
- API Keys séparées par environnement
- CORS configuré pour domaines autorisés

#### 1.2 Database Schema
**Objectif :** Structure de données complète et évolutive

```sql
-- Users (extends Supabase auth.users)
CREATE TABLE profiles (
  id UUID REFERENCES auth.users(id) PRIMARY KEY,
  email TEXT UNIQUE NOT NULL,
  full_name TEXT,
  avatar_url TEXT,
  timezone TEXT DEFAULT 'Europe/Paris',
  preferences JSONB DEFAULT '{}',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Polls
CREATE TABLE polls (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  creator_id UUID REFERENCES profiles(id) NOT NULL,
  title TEXT NOT NULL,
  description TEXT,
  slug TEXT UNIQUE NOT NULL, -- pour URLs friendly
  settings JSONB DEFAULT '{}', -- config granularité, notifications, etc.
  status TEXT DEFAULT 'draft' CHECK (status IN ('draft', 'active', 'closed', 'archived')),
  expires_at TIMESTAMP WITH TIME ZONE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Poll Options (dates sélectionnées)
CREATE TABLE poll_options (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  poll_id UUID REFERENCES polls(id) ON DELETE CASCADE,
  option_date DATE NOT NULL,
  time_slots JSONB, -- [{hour: 14, minute: 0, duration: 60}, ...]
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Votes
CREATE TABLE votes (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  poll_id UUID REFERENCES polls(id) ON DELETE CASCADE,
  voter_email TEXT NOT NULL,
  voter_name TEXT,
  voter_id UUID REFERENCES profiles(id), -- NULL si anonyme
  selections JSONB NOT NULL, -- {option_id: "yes|no|maybe", ...}
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  UNIQUE(poll_id, voter_email)
);

-- Messages IA (conversations)
CREATE TABLE conversations (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  user_id UUID REFERENCES profiles(id),
  session_id TEXT NOT NULL, -- pour utilisateurs anonymes
  messages JSONB NOT NULL, -- [{role: "user|assistant", content: "...", timestamp}]
  context JSONB DEFAULT '{}', -- données extraites (dates, participants, etc.)
  poll_id UUID REFERENCES polls(id), -- NULL si pas encore créé
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Analytics
CREATE TABLE analytics_events (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  event_type TEXT NOT NULL,
  event_data JSONB NOT NULL,
  user_id UUID REFERENCES profiles(id),
  session_id TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

**Indexes :**
```sql
CREATE INDEX idx_polls_creator ON polls(creator_id);
CREATE INDEX idx_polls_slug ON polls(slug);
CREATE INDEX idx_poll_options_poll ON poll_options(poll_id);
CREATE INDEX idx_votes_poll ON votes(poll_id);
CREATE INDEX idx_conversations_user ON conversations(user_id);
CREATE INDEX idx_conversations_session ON conversations(session_id);
CREATE INDEX idx_analytics_type ON analytics_events(event_type);
```

**Row Level Security (RLS) :**
```sql
-- Profiles : utilisateur peut voir/modifier son profil uniquement
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users can view own profile" ON profiles FOR SELECT USING (auth.uid() = id);
CREATE POLICY "Users can update own profile" ON profiles FOR UPDATE USING (auth.uid() = id);

-- Polls : créateur peut tout, autres peuvent voir si actif
ALTER TABLE polls ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Creator can manage polls" ON polls USING (auth.uid() = creator_id);
CREATE POLICY "Anyone can view active polls" ON polls FOR SELECT USING (status = 'active');

-- Poll Options : même logique que polls
ALTER TABLE poll_options ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Poll options follow poll access" ON poll_options FOR SELECT 
  USING (EXISTS (SELECT 1 FROM polls WHERE polls.id = poll_id AND (polls.creator_id = auth.uid() OR polls.status = 'active')));

-- Votes : voter peut voir/modifier ses votes, créateur peut voir tous les votes
ALTER TABLE votes ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Voters can manage own votes" ON votes USING (voter_email = auth.email() OR voter_id = auth.uid());
CREATE POLICY "Poll creators can view all votes" ON votes FOR SELECT 
  USING (EXISTS (SELECT 1 FROM polls WHERE polls.id = poll_id AND polls.creator_id = auth.uid()));

-- Conversations : utilisateur peut voir ses conversations
ALTER TABLE conversations ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users can manage own conversations" ON conversations USING (auth.uid() = user_id);
```

#### 1.3 Authentication
**Objectif :** Système d'auth flexible (connecté + anonyme)

**Providers configurés :**
- **Google OAuth** (priorité - 80% des utilisateurs)
- **Email/Password** (backup - 20% des utilisateurs)
- **Anonymous** (pour votes sans inscription)

**Configuration Google OAuth :**
```javascript
// supabase/config.js
const supabaseConfig = {
  auth: {
    providers: {
      google: {
        redirectTo: `${window.location.origin}/auth/callback`
      }
    },
    flowType: 'pkce' // Plus sécurisé
  }
}
```

**Flux d'authentification :**
1. **Créateur de sondage** : Auth obligatoire (Google ou Email)
2. **Votant** : Auth optionnelle (peut voter avec juste email)
3. **Session anonyme** : UUID temporaire pour conversations IA

---

### Semaines 3-4 : Interface de Vote Mobile + IA Basique

#### 2.1 OpenAI Integration (NOUVELLE PRIORITÉ)
**Objectif :** Chat → Sondage automatique

**Configuration OpenAI :**
- **Modèle :** `gpt-4-turbo` (meilleur reasoning)
- **Backup :** `gpt-3.5-turbo` (coût réduit)
- **Streaming :** Activé pour UX fluide
- **Context window :** 128k tokens (conversations longues)

**Variables d'environnement :**
```env
OPENAI_API_KEY=your_key_here
OPENAI_MODEL=gpt-4-turbo
OPENAI_MAX_TOKENS=1000
OPENAI_TEMPERATURE=0.1
```

#### 2.2 Interface de Vote Mobile
**Objectif :** Interface de vote mobile-native révolutionnaire

**Composants Vote Mobile :**
```typescript
// components/voting/mobile/
├── VotingInterfaceTouch.tsx    // Interface tactile
├── GesturesHandler.tsx         // Gestures avancées
├── HapticFeedback.tsx         // Retour haptique
└── VoteAnimations.tsx         // Micro-animations
```

#### 2.3 Function Calling IA Basique
**Objectif :** Conversion automatique langage naturel → sondage

**Functions définies :**
```typescript
// ai/functions.ts
export const openAIFunctions = [
  {
    name: "create_poll_draft",
    description: "Crée un brouillon de sondage à partir des informations extraites",
    parameters: {
      type: "object",
      properties: {
        title: {
          type: "string",
          description: "Titre du sondage généré automatiquement"
        },
        dates: {
          type: "array",
          items: { type: "string", format: "date" },
          description: "Dates proposées extraites de la conversation"
        },
        timeSlots: {
          type: "object",
          description: "Créneaux horaires par date si mentionnés"
        },
        participants: {
          type: "array",
          items: { type: "string", format: "email" },
          description: "Emails des participants mentionnés"
        },
        context: {
          type: "object",
          description: "Contexte additionnel (lieu, type de réunion, etc.)"
        }
      },
      required: ["title", "dates"]
    }
  },
  {
    name: "ask_clarification",
    description: "Demande des clarifications à l'utilisateur",
    parameters: {
      type: "object",
      properties: {
        question: {
          type: "string",
          description: "Question à poser pour clarifier"
        },
        options: {
          type: "array",
          items: { type: "string" },
          description: "Options suggérées si applicable"
        }
      },
      required: ["question"]
    }
  }
];
```

---

### Semaines 5-6 : Dashboard & IA Avancée

#### 3.1 Dashboard Mobile
**Objectif :** Interface de gestion mobile-optimisée

**Composants Dashboard :**
```typescript
// components/dashboard/mobile/
├── DashboardMobile.tsx     // Interface principale
├── PollsListMobile.tsx     // Liste sondages tactile
├── QuickActions.tsx        // Actions rapides
├── StatsWidgets.tsx        // Statistiques visuelles
└── MobileNavigation.tsx    // Navigation mobile
```

#### 3.2 IA Conversationnelle Avancée
**Objectif :** IA complète avec parsing avancé

**Fonctionnalités IA avancées :**
```typescript
// ai/advanced/
├── ContextManager.ts       // Gestion contexte conversation
├── DateTimeParser.ts       // Parsing dates/heures NLP
├── SmartSuggestions.ts     // Suggestions intelligentes
├── LearningEngine.ts       // Apprentissage préférences
└── ConversationFlow.ts     // Flow conversation optimisé
```

**Parsing avancé :**
- "mercredi prochain" → date exacte
- "15h-17h" → créneaux horaires
- "Paul et Marie" → participants
- "réunion équipe" → contexte métier
```

---

### Semaines 7-8 : Polish & Beta Launch

#### 4.1 Dashboard Utilisateur
**Objectif :** Interface de gestion des sondages

**Sections Dashboard :**
- **Brouillons** : Créés mais pas encore envoyés
- **Actifs** : En cours de vote
- **Terminés** : Vote clos, résultats disponibles
- **Archivés** : Anciens sondages

#### 4.2 Tests E2E
**Objectif :** Validation complète des flux utilisateur

**Scénarios de test (Playwright) :**
- Création sondage (avec/sans horaires)
- Vote anonyme et connecté
- Modifications temps réel
- Notifications email
- Responsive mobile
- Performance (< 2s chargement)

#### 4.3 Beta Launch
**Objectif :** Lancement avec 50 early adopters

**Stratégie de lancement :**
1. **Invitations privées** : 50 contacts ciblés
2. **Onboarding guidé** : Tutorial interactif
3. **Feedback loop** : Formulaire intégré + analytics
4. **Support réactif** : Discord/email pour bugs
5. **Itérations rapides** : Hotfixes sous 24h

---

## Phase 4 : Application Mobile Native (Capacitor)

### Semaines 9-12 : Développement Mobile Android

#### 4.1 Setup Capacitor (Semaine 9)
**Objectif :** Transformer l'app web en application Android native

**Installation et configuration :**
```bash
# Installation Capacitor
npm install @capacitor/core @capacitor/cli @capacitor/android

# Configuration projet
npx cap init DooDates app.doodates.com
npx cap add android

# Premier build Android
npm run build && npx cap copy && npx cap open android
```

**Configuration technique :**
```typescript
// capacitor.config.ts
const config: CapacitorConfig = {
  appId: 'app.doodates.com',
  appName: 'DooDates',
  webDir: 'dist',
  plugins: {
    SplashScreen: {
      launchShowDuration: 2000,
      backgroundColor: "#3b82f6"
    },
    PushNotifications: {
      presentationOptions: ["badge", "sound", "alert"]
    }
  }
};
```

#### 4.2 APIs Natives (Semaine 10)
**Objectif :** Intégration fonctionnalités natives Android

**Services natifs implémentés :**
```typescript
// services/native.ts
export class NativeService {
  // Partage natif
  static async shareUrl(url: string, title: string) {
    await Share.share({
      title,
      url,
      dialogTitle: 'Partager le sondage'
    });
  }

  // Notifications locales
  static async scheduleNotification(poll: Poll) {
    await LocalNotifications.schedule({
      notifications: [{
        title: 'Rappel sondage',
        body: `N'oubliez pas de voter pour "${poll.title}"`,
        schedule: { at: new Date(poll.reminder_date) }
      }]
    });
  }

  // Intégration calendrier
  static async addToCalendar(poll: Poll, slot: TimeSlot) {
    await Calendar.createEvent({
      title: poll.title,
      startDate: slot.start,
      endDate: slot.end,
      notes: `Créé via DooDates: ${poll.url}`
    });
  }
}
```

#### 4.3 Notifications Push (Semaine 11)
**Objectif :** Système de notifications push complet

**Architecture notifications :**
```typescript
// services/pushNotifications.ts
export class PushService {
  static async initialize() {
    const permission = await PushNotifications.requestPermissions();
    
    if (permission.receive === 'granted') {
      await PushNotifications.register();
    }

    // Sauvegarde token dans Supabase
    PushNotifications.addListener('registration', (token) => {
      this.saveTokenToSupabase(token.value);
    });
  }

  // Envoi via Supabase Edge Functions
  static async sendPollNotification(pollId: string, message: string) {
    await supabase.functions.invoke('send-push-notification', {
      body: { pollId, message }
    });
  }
}
```

#### 4.4 Optimisations Mobile (Semaine 12)
**Objectif :** Performance et UX mobile optimisées

**Adaptations UI spécifiques :**
```typescript
// hooks/useCapacitor.ts
export const useCapacitor = () => {
  const isNative = Capacitor.isNativePlatform();
  const platform = Capacitor.getPlatform();
  
  return {
    isNative,
    isAndroid: platform === 'android',
    adaptiveClassName: isNative ? 'pt-safe-area-top pb-safe-area-bottom' : ''
  };
};
```

**Gestion offline avancée :**
```typescript
// services/offline.ts
export class OfflineService {
  static async saveDraft(poll: PollDraft) {
    if (Capacitor.isNativePlatform()) {
      // SQLite natif pour performance
      await CapacitorSQLite.executeSet({
        database: 'doodates',
        set: [{
          statement: 'INSERT INTO drafts VALUES (?, ?)',
          values: [poll.id, JSON.stringify(poll)]
        }]
      });
    } else {
      localStorage.setItem(`draft_${poll.id}`, JSON.stringify(poll));
    }
  }
}
```

---

### Architecture Mobile Complète

#### Stack Technologique Mobile
```
DooDates React App (100% réutilisé)
├── Web → Vercel (production)
├── PWA → Service Worker (existant)
└── Mobile → Capacitor → Android APK
    ├── APIs Natives (Share, Calendar, Notifications)
    ├── SQLite Local (Offline)
    └── Push Notifications (Firebase)
```

#### Workflow de Développement
```json
{
  "scripts": {
    "build:android": "npm run build && npx cap copy android",
    "android:dev": "npx cap run android",
    "android:build": "npx cap build android --prod",
    "android:deploy": "fastlane android deploy"
  }
}
```

#### CI/CD Android
```yaml
# .github/workflows/android-build.yml
name: Android Build & Deploy
on:
  push:
    tags: ['v*']

jobs:
  android:
    runs-on: ubuntu-latest
    steps:
      - name: Build Android APK
        run: |
          npm run build
          npx cap copy android
          npx cap build android --prod
      
      - name: Deploy to Play Store
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.GOOGLE_PLAY_SERVICE_ACCOUNT }}
          packageName: app.doodates.com
          releaseFiles: android/app/build/outputs/bundle/release/*.aab
```

---

### Avantages Stratégiques Mobile

#### vs Concurrents
- **Doodle** : Pas d'app native, seulement PWA basique
- **Framadate** : Aucune app mobile
- **DooDates** : App native complète avec notifications push

#### Métriques Attendues
- **Engagement** : +40% vs web (notifications push)
- **Rétention** : +25% (app installée sur device)
- **Conversion** : +15% (UX native optimisée)
- **Time-to-market** : 4 semaines (vs 12 semaines Flutter)

#### ROI Mobile
- **Coût développement** : 1 mois développeur
- **Maintenance** : 0% supplémentaire (même codebase)
- **Revenue impact** : +30% utilisateurs premium attendus
- **Competitive advantage** : 6-12 mois d'avance sur concurrents

---

### Timeline Mobile Intégrée

**Semaine 9 :** Setup Capacitor + premier build Android  
**Semaine 10 :** APIs natives (Share, Calendar, Notifications locales)  
**Semaine 11 :** Push notifications + intégration Supabase  
**Semaine 12 :** Optimisations + soumission Play Store  

**Livrable final :** Application Android native DooDates disponible sur Google Play Store, partageant 100% du code avec l'application web.
