# 🤖 Contexte pour l'IA - Monitoring des Workflows

## 📋 Fichiers de Monitoring

Lorsque vous travaillez sur ce projet, consultez automatiquement ces fichiers pour comprendre l'état de santé du CI/CD :

1. **`Docs/monitoring/workflow-status.json`** - Statut rapide (JSON) pour vérification rapide
2. **`Docs/monitoring/workflow-failures-report.md`** - Rapport détaillé complet

## 🔍 Quand Consulter

### Consultation Automatique Requise

Consultez automatiquement le monitoring dans ces cas :

1. **Quand l'utilisateur mentionne des problèmes de CI/CD** ou des workflows qui échouent
2. **Quand vous voyez une issue GitHub avec le label `ci-health`** ou `ci-failure`
3. **Quand vous travaillez sur des fichiers de workflow** (`.github/workflows/*.yml`)
4. **Quand l'utilisateur demande de l'aide** pour résoudre des problèmes de build/test
5. **Avant de proposer des changements majeurs** qui pourraient affecter les workflows

### Consultation Proactive

Si vous détectez des patterns d'échecs dans les conversations ou les fichiers, consultez le rapport pour :
- Identifier les workflows en échec récurrent
- Proposer des solutions basées sur l'historique
- Comprendre les tendances de santé du CI/CD

## 📊 Structure des Fichiers

### workflow-status.json
```json
{
  "lastUpdate": "ISO date",
  "totalFailures24h": 0,
  "totalFailures7d": 0,
  "workflowsMonitored": 6,
  "hasFailures": false,
  "reportPath": "Docs/monitoring/workflow-failures-report.md"
}
```

### workflow-failures-report.md
Rapport markdown détaillé avec :
- Statut de chaque workflow
- Échecs récents (24h et 7 jours)
- Détails des jobs en échec
- Recommandations

## 🚨 Alertes Automatiques

Le système crée automatiquement une issue GitHub (`ci-health` label) quand il y a des échecs dans les 24h.

**Action requise** : Quand vous voyez une issue avec le label `ci-health`, consultez immédiatement :
1. Le rapport détaillé (`workflow-failures-report.md`)
2. Les liens vers les runs GitHub dans le rapport
3. Proposez des solutions basées sur les patterns d'échecs

## 💡 Exemple d'Utilisation

```javascript
// Lire le statut rapide
const status = await readFile('Docs/monitoring/workflow-status.json');
if (status.hasFailures) {
  // Lire le rapport détaillé
  const report = await readFile('Docs/monitoring/workflow-failures-report.md');
  // Analyser et proposer des solutions
}
```

## 🔄 Mise à Jour

Le monitoring est mis à jour :
- **Toutes les heures** (schedule)
- **Après chaque workflow important** (workflow_run trigger)
- **Manuellement** (workflow_dispatch)

Le rapport est toujours à jour dans le dépôt.

