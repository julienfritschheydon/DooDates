        name: 🧪 Test Branch - Tests Dashboard Corrigés

on:
  push:
    branches: [test, test-dashboard]
  workflow_dispatch: # Permet de lancer manuellement depuis GitHub UI

concurrency:
  group: test-branch-${{ github.ref }}
  cancel-in-progress: true

jobs:
          # Job unique: Tests dashboard corrigés
          e2e-isolated:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 🎭 Cache Playwright browsers
        id: cache-playwright
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: 🔧 Install dependencies
        run: npm ci

      - name: 🧭 Install Playwright (Chromium only)
        run: |
          if [ "${{ steps.cache-playwright.outputs.cache-hit }}" == "true" ]; then
            echo "✅ Cache trouvé, installation rapide (sans deps)..."
            npx playwright install chromium
          else
            echo "⚠️ Cache vide, installation avec dépendances système..."
            npx playwright install --with-deps chromium
          fi

              - name: 🎯 Run Dashboard Tests (Tests corrigés)
                run: |
                  npx playwright test \
                    tests/e2e/dashboard-complete.spec.ts \
                    tests/e2e/tags-folders.spec.ts \
                    --config=playwright.config.optimized.ts \
                    --project=chromium \
                    --grep="@functional - (Sélectionner|Assigner)" \
                    --reporter=html,json
        env:
          CI: true
          VITE_GEMINI_API_KEY: fake-key-for-e2e-tests

              - name: 📊 Upload Playwright HTML report
                if: always()
                uses: actions/upload-artifact@v4
                with:
                  name: playwright-report-dashboard
                  path: playwright-report
                  retention-days: 3

              - name: 📤 Upload test results JSON
                if: always()
                uses: actions/upload-artifact@v4
                with:
                  name: test-results-dashboard
                  path: test-results
                  retention-days: 3

      - name: 📸 Upload screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-screenshots
          path: test-results/**/*.png
          if-no-files-found: ignore
          retention-days: 3

      - name: 🎥 Upload videos
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-videos
          path: test-results/**/*.webm
          if-no-files-found: ignore
          retention-days: 3

      - name: 📋 Upload traces
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-traces
          path: test-results/**/*.zip
          if-no-files-found: ignore
          retention-days: 3

          # Summary avec commentaire
          test-summary:
            needs: [e2e-isolated]
            runs-on: ubuntu-latest
            if: always()
            steps:
              - name: 📊 Test Summary
                uses: actions/github-script@v7
                with:
                  script: |
                    const status = '${{ needs.e2e-isolated.result }}' === 'success' ? '✅' : '❌';
                    
                    let summary = `## ${status} Tests Dashboard - ${status === '✅' ? 'Réussis' : 'Échoués'}\n\n`;
                    summary += `**Branche:** \`${context.ref.replace('refs/heads/', '')}\`\n`;
                    summary += `**Objectif:** Tester les tests corrigés (Sélectionner, Assigner tags/dossiers)\n\n`;
                    
                    if (status === '✅') {
                      summary += '🚀 **Les tests passent !**\n';
                      summary += '📸 **Screenshots disponibles dans les artefacts pour debug**';
                    } else {
                      summary += '⚠️ **Des tests ont échoué. Vérifier les rapports et screenshots dans les artefacts.**';
                    }
                    
                    summary += `\n\n📋 **Rapports détaillés disponibles dans les artefacts GitHub Actions**`;
                    
                    // Afficher le summary dans les logs (pas de commentaire GitHub pour éviter les erreurs de permissions)
                    console.log(summary);
