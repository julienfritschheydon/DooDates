name: 🎯 Pre-prod → Main (Auto-merge)

on:
  push:
    branches:
      - pre-prod

permissions:
  contents: write

jobs:
  preprod-validation:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          
      - name: 📦 Install Dependencies
        run: npm ci
        
      # ⚡ TEMPORAIREMENT DÉSACTIVÉ POUR DEBUG E2E
      # On remettra après résolution du problème
      
      # - name: 🧪 Tests Unitaires Complets
      #   run: npm run test:unit:fast
      #   timeout-minutes: 5
        
      # - name: 📝 TypeScript Check
      #   run: npm run type-check
      #   timeout-minutes: 3
        
      # - name: 🧹 Linting & Formatage
      #   run: npm run lint -- --max-warnings=0
      #   timeout-minutes: 3
        
      # - name: 🏗️ Build Validation
      #   run: npm run build
      #   timeout-minutes: 5
        
      # - name: 🎭 Cache Playwright browsers
      #   id: cache-playwright
      #   uses: actions/cache@v4
      #   with:
      #     path: ~/.cache/ms-playwright
      #     key: ${{ runner.os }}-playwright-${{ hashFiles('package-lock.json', 'playwright.config*.ts') }}
      #     restore-keys: |
      #       ${{ runner.os }}-playwright-
      
      # - name: 🧭 Install Playwright (for debug test)
      #   run: |
      #     echo "🔧 Installation de Playwright pour le test de debug CI"
      #     npx playwright install --with-deps
      #   timeout-minutes: 3
      
      # 🔍 FIN DES ÉTAPES DÉSACTIVÉES
      
      - name: 🔍 CI Debug Test - SEUL TEST NÉCESSAIRE
        run: |
          echo "🔍 DÉBUT DE L'ANALYSE CI DEBUG - Chat Input"
          echo "Ce test s'exécute en premier pour identifier les problèmes CI"
          echo "Si ce test échoue, tous les autres tests E2E échoueront aussi"
          echo ""
          # IMPORTANT: Force NODE_ENV=development for E2E tests
          # This ensures React renders the complete interface with data-testid elements
          CI=true NODE_ENV=development BASE_URL=http://localhost:8080/DooDates VITE_GEMINI_API_KEY=TEST_MODE npx playwright test tests/e2e/ci-debug-chat-input.spec.ts --project=chromium --reporter=list
        timeout-minutes: 5
        continue-on-error: false
        
      - name: 📸 Upload CI Debug Screenshots (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ci-debug-screenshots-${{ github.run_number }}
          path: |
            test-results/**/*.png
            ci-debug-*.png
          retention-days: 7
        
      - name: 📊 CI Debug Summary
        if: always()
        run: |
          echo "## CI Debug Summary" >> $GITHUB_STEP_SUMMARY
          echo "🔍 Focus: CI Debug Test UNIQUEMENT" >> $GITHUB_STEP_SUMMARY
          echo "⚡ Durée: ~2 minutes" >> $GITHUB_STEP_SUMMARY
          echo "🎯 Objectif: Diagnostiquer React en CI" >> $GITHUB_STEP_SUMMARY

  auto-merge-to-main:
    needs: preprod-validation
    runs-on: ubuntu-latest
    if: success() && github.event_name == 'push' && github.ref == 'refs/heads/pre-prod'
    steps:
      - name: 📥 Checkout Main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🔀 Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: 🚀 Merge Pre-prod to Main
        run: |
          git merge origin/pre-prod --no-ff -m "chore: auto-merge pre-prod to main [production release]"
          git push origin main
          
      - name: 🏷️ Create Release Tag
        run: |
          TAG_NAME="release-$(date +%Y%m%d-%H%M%S)"
          git tag -a "$TAG_NAME" -m "Production release from pre-prod"
          git push origin "$TAG_NAME"
          echo "🏷️ Created release tag: $TAG_NAME" >> $GITHUB_STEP_SUMMARY
