name: 1️⃣ PR Complete Validation

on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [test/workflows-validation]

jobs:
  # Job 1: Tests Rapides (Parallèle)
  quick-tests:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        test-type: [unit, integration, ux-regression]
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4
      
      - name: 📦 Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run ${{ matrix.test-type }} tests
        run: npm run test:${{ matrix.test-type }}
      
      - name: Upload test results
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.test-type }}
          path: test-results/
      
      - name: Upload coverage
        if: matrix.test-type == 'unit'
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage/lcov.info

  # Job 2: Tests IA (Critique)
  ai-validation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run AI Tests (Quick)
        run: npm run test:gemini:quick
        env:
          VITE_GEMINI_API_KEY: ${{ secrets.VITE_GEMINI_API_KEY }}
      
      - name: Validate AI Performance
        run: |
          if [ ! -f ./tests/reports/quick-report.json ]; then
            echo "Aucun rapport AI détecté (tests IA ignorés). Validation AI sautée."
            exit 0
          fi
          SCORE=$(node -e "
            const fs = require('fs');
            const report = require('./tests/reports/quick-report.json');
            console.log(report.percentage || 0);
          ")
          echo "Score IA: $SCORE%"
          if (( $(echo "$SCORE < 70" | bc -l) )); then
            echo "Performance IA insuffisante: $SCORE%"
            exit 1
          fi
          echo "Performance IA validée: $SCORE%"
      
      - name: Upload AI test results
        uses: actions/upload-artifact@v4
        with:
          name: ai-test-results
          path: tests/reports/

  # Job 3: Build & Type Check
  build-validation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: TypeScript Check
        run: npm run type-check
      
      - name: Build production
        run: npm run build
      
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: dist/

  # Job 4: Code Quality
  code-quality:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Lint check
        run: npm run lint -- --max-warnings 1000
      
      - name: Format check
        run: npm run format:check
      
      - name: Security audit
        run: npm audit --audit-level moderate

  # Job 5: E2E Smoke (Chromium only - Tests critiques rapides)
  e2e-smoke:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 🔧 Install dependencies
        run: npm ci

      - name: 🧭 Install Playwright (Chromium only)
        run: npx playwright install --with-deps chromium

      - name: 🔥 Run Smoke Tests (Critical paths only)
        run: npm run test:e2e:smoke -- --reporter=html,json
        env:
          VITE_GEMINI_API_KEY: fake-key-for-e2e-tests  # Mock will intercept API calls

      - name: 📊 Upload Playwright HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-smoke
          path: playwright-report
          retention-days: 7

      - name: 📤 Upload test results JSON
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-smoke
          path: test-results
          retention-days: 7

  # Job 5b: E2E Functional (Chromium only - Tests fonctionnels complets)
  e2e-functional:
    runs-on: ubuntu-latest
    needs: [e2e-smoke]  # Exécuter après smoke tests
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 🔧 Install dependencies
        run: npm ci

      - name: 🧭 Install Playwright (Chromium only)
        run: npx playwright install --with-deps chromium

      - name: ⚡ Run Functional Tests (Complete user flows)
        run: npm run test:e2e:functional -- --reporter=html,json
        env:
          VITE_GEMINI_API_KEY: fake-key-for-e2e-tests  # Mock will intercept API calls

      - name: 📊 Upload Playwright HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-functional
          path: playwright-report
          retention-days: 7

      - name: 📤 Upload test results JSON
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-functional
          path: test-results
          retention-days: 7

  # Job 5c: E2E Matrix (Desktop + Mobile - Tests multi-navigateurs)
  e2e-matrix:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        project: [
          'chromium',
          'firefox',
          'webkit',
          'Mobile Chrome',
          'Mobile Safari'
        ]
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 🔧 Install dependencies
        run: npm ci

      - name: 🧭 Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: 🧪 Run Playwright E2E (${{ matrix.project }})
        run: |
          npx playwright test --project="${{ matrix.project }}" --reporter=html,json --ignore-snapshots --grep-invert "Edge Cases and Error Handling|Guest User Workflow|Authenticated User Workflow"

      - name: 📊 Upload Playwright report (${{ matrix.project }})
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-${{ matrix.project }}
          path: playwright-report
          retention-days: 7

      - name: 📤 Upload test results JSON (${{ matrix.project }})
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.project }}
          path: test-results
          retention-days: 7

  # Job 6: Summary
  validation-summary:
    needs: [quick-tests, ai-validation, build-validation, code-quality, e2e-smoke, e2e-functional, e2e-matrix]
    runs-on: ubuntu-latest
    if: always() && github.event_name == 'pull_request'

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 🔧 Install dependencies
        run: npm ci

      - name: 📥 Download test results (if available)
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          pattern: test-results-*
          merge-multiple: true
          path: test-results

      - name: 📊 Generate E2E Summary (if failures)
        id: e2e-summary
        if: needs.e2e-smoke.result == 'failure' || needs.e2e-functional.result == 'failure' || needs.e2e-matrix.result == 'failure'
        run: |
          if [ -d test-results ]; then
            node scripts/generate-regression-summary.js || echo "Summary generation failed"
          else
            echo "No test results available"
          fi
        continue-on-error: true

      - name: 📊 Validation Summary
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = require('path');
            
            const jobs = [
              { name: 'Tests Rapides', status: '${{ needs.quick-tests.result }}' },
              { name: 'Validation IA', status: '${{ needs.ai-validation.result }}' },
              { name: 'Build & Types', status: '${{ needs.build-validation.result }}' },
              { name: 'Code Quality', status: '${{ needs.code-quality.result }}' },
              { name: 'E2E Smoke (Critical)', status: '${{ needs.e2e-smoke.result }}' },
              { name: 'E2E Functional (Complete)', status: '${{ needs.e2e-functional.result }}' },
              { name: 'E2E Matrix (All Browsers)', status: '${{ needs.e2e-matrix.result }}' }
            ];
            
            const passed = jobs.filter(j => j.status === 'success').length;
            const total = jobs.length;
            const status = passed === total ? '✅' : '❌';
            
            let summary = `## ${status} Validation PR - ${passed}/${total} jobs réussis\n\n`;
            summary += `${jobs.map(j => `- ${j.status === 'success' ? '✅' : '❌'} ${j.name}`).join('\n')}\n\n`;
            
            // Ajouter le résumé E2E détaillé si disponible
            const summaryPath = path.join(process.cwd(), 'regression-summary.md');
            if (fs.existsSync(summaryPath)) {
              const e2eSummary = fs.readFileSync(summaryPath, 'utf-8');
              summary += `\n---\n\n## 🔍 Détails des Tests E2E\n\n${e2eSummary}\n`;
            }
            
            summary += `\n---\n\n`;
            summary += passed === total ? 
              '🚀 **Toutes les validations sont passées ! Cette PR peut être mergée.**' : 
              '⚠️ **Des validations ont échoué. Veuillez corriger avant de merger.**\n\n';
            summary += `📋 **Rapports détaillés disponibles dans les artefacts GitHub Actions**`;
            
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: summary
            }); 