name: Production Deploy [LEGACY]

# LEGACY: ce workflow de déploiement production est remplacé par
# la chaîne complète "3-main-validation.yml" → "4-main-deploy-pages.yml" → "5-production-smoke-tests.yml"
# qui gère validation, déploiement GitHub Pages et smoke tests prod.
#
# Ce workflow ne fait pas de vrai déploiement et est conservé pour référence
# mais désactivé via `if: false`.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  production-deploy:
    runs-on: ubuntu-latest
    if: false
    
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        
      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          
      - name: 📦 Install Dependencies
        run: npm ci
        
      - name: 🏗️ Build Production
        run: npm run build
        
      - name: 🚀 Deploy to Production
        run: |
          echo "🚀 Deploying to production..."
          echo "🌐 URL: https://julienfritschheydon.github.io/DooDates/"
          # Note: GitHub Pages deployment will be handled by existing workflow
        
      - name: ❤️ Health Check
        run: |
          echo "❤️ Running production health checks..."
          echo "📊 Monitoring active"
          echo "🔍 Error tracking enabled"
        
      - name: 📊 Deploy Summary
        if: always()
        run: |
          echo "## Production Deploy Summary" >> $GITHUB_STEP_SUMMARY
          echo "✅ Build: Production ready" >> $GITHUB_STEP_SUMMARY
          echo "✅ Deploy: Live on GitHub Pages" >> $GITHUB_STEP_SUMMARY
          echo "✅ Health: Monitoring active" >> $GITHUB_STEP_SUMMARY
          echo "🌐 URL: https://julienfritschheydon.github.io/DooDates/" >> $GITHUB_STEP_SUMMARY
          echo "📝 Phase: Production (monitoring continu)" >> $GITHUB_STEP_SUMMARY
