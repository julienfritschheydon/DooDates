name: Tests Automatisés Gemini

on:
  # DÉSACTIVÉ pour économiser coûts API Gemini
  # Lancer manuellement uniquement via workflow_dispatch
  # schedule:
  #   - cron: '0 9 * * 1'
  workflow_dispatch: # MANUEL UNIQUEMENT
  # push:
  #   branches: [ main ]
  #   paths: 
  #     - 'src/lib/gemini.ts'
  #     - 'src/lib/enhanced-gemini.ts'
  #     - 'tests/gemini-*.test.ts'

jobs:
  test-gemini:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Run Gemini tests
      env:
        VITE_GEMINI_API_KEY: ${{ secrets.VITE_GEMINI_API_KEY }}
      run: npm run test:gemini
      
    - name: Upload test reports
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: gemini-test-reports
        path: tests/reports/
        retention-days: 30
        
    - name: Comment PR with results
      if: false  # Disabled: no PR context in current triggers
      uses: actions/github-script@v7
      with:
        script: |
          const fs = require('fs');
          try {
            const reportPath = 'tests/reports/gemini-test-report.md';
            if (fs.existsSync(reportPath)) {
              const report = fs.readFileSync(reportPath, 'utf8');
              github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: '## 🤖 Tests Automatisés Gemini\n\n' + report
              });
            }
          } catch (error) {
            console.log('Pas de rapport à publier:', error.message);
          }
          
    - name: Create Issue on Failure
      if: failure()
      uses: actions/github-script@v7
      with:
        script: |
          github.rest.issues.create({
            owner: context.repo.owner,
            repo: context.repo.repo,
            title: '🚨 Tests Automatisés Gemini - Échec Critique',
            body: `## Tests Gemini en Échec
            
            **Date:** ${new Date().toISOString()}
            **Workflow:** ${context.workflow}
            **Run ID:** ${context.runId}
            
            Les tests automatisés Gemini ont échoué. Veuillez vérifier :
            - La connexion à l'API Gemini
            - Les quotas API
            - Les changements récents dans le code
            
            [Voir les détails du run](https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})
            `,
            labels: ['bug', 'tests', 'gemini', 'urgent']
          }); 